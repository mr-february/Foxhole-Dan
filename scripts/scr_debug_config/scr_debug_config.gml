// Flip to false before building for the Vercel push — the level-select menu
// (title screen, T key) is a local dev/testing aid only and should never
// ship in the public build.
#macro DEBUG_LEVEL_SELECT true
